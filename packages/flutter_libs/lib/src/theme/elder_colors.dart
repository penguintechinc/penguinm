import 'package:flutter/material.dart';

/// Elder theme color constants matching the Tailwind slate/amber palette.
///
/// These mirror the color values used in the react-libs Elder theme.
abstract final class ElderColors {
  /// Lightest shade of the slate palette (50).
  static const Color slate50 = Color(0xFFF8FAFC);

  /// Light shade of the slate palette (100).
  static const Color slate100 = Color(0xFFF1F5F9);

  /// Light shade of the slate palette (200).
  static const Color slate200 = Color(0xFFE2E8F0);

  /// Light shade of the slate palette (300).
  static const Color slate300 = Color(0xFFCBD5E1);

  /// Medium-light shade of the slate palette (400).
  static const Color slate400 = Color(0xFF94A3B8);

  /// Medium shade of the slate palette (500).
  static const Color slate500 = Color(0xFF64748B);

  /// Medium-dark shade of the slate palette (600).
  static const Color slate600 = Color(0xFF475569);

  /// Dark shade of the slate palette (700).
  static const Color slate700 = Color(0xFF334155);

  /// Dark shade of the slate palette (800).
  static const Color slate800 = Color(0xFF1E293B);

  /// Very dark shade of the slate palette (900).
  static const Color slate900 = Color(0xFF0F172A);

  /// Darkest shade of the slate palette (950).
  static const Color slate950 = Color(0xFF020617);

  /// Lightest shade of the amber palette (50).
  static const Color amber50 = Color(0xFFFFFBEB);

  /// Light shade of the amber palette (100).
  static const Color amber100 = Color(0xFFFEF3C7);

  /// Light shade of the amber palette (200).
  static const Color amber200 = Color(0xFFFDE68A);

  /// Light shade of the amber palette (300).
  static const Color amber300 = Color(0xFFFCD34D);

  /// Medium-light shade of the amber palette (400).
  static const Color amber400 = Color(0xFFFBBF24);

  /// Medium shade of the amber palette (500).
  static const Color amber500 = Color(0xFFF59E0B);

  /// Medium-dark shade of the amber palette (600).
  static const Color amber600 = Color(0xFFD97706);

  /// Dark shade of the amber palette (700).
  static const Color amber700 = Color(0xFFB45309);

  /// Dark shade of the amber palette (800).
  static const Color amber800 = Color(0xFF92400E);

  /// Very dark shade of the amber palette (900).
  static const Color amber900 = Color(0xFF78350F);

  /// Error color shade (red 400).
  static const Color red400 = Color(0xFFF87171);

  /// Error color shade (red 500).
  static const Color red500 = Color(0xFFEF4444);

  /// Error color shade (red 600).
  static const Color red600 = Color(0xFFDC2626);

  /// Success color shade (green 400).
  static const Color green400 = Color(0xFF4ADE80);

  /// Success color shade (green 500).
  static const Color green500 = Color(0xFF22C55E);

  /// Informational color shade (blue 400).
  static const Color blue400 = Color(0xFF60A5FA);

  /// Informational color shade (blue 500).
  static const Color blue500 = Color(0xFF3B82F6);

  /// Pure white color.
  static const Color white = Color(0xFFFFFFFF);

  /// Pure black color.
  static const Color black = Color(0xFF000000);
}
