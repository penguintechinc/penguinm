# penguin_api

`PenguinApiClient`: `package:http`-based client with `AuthClient`,
`RetryClient`, `TraceClient` and `SanitizedLogClient` middleware layers,
`RetryPolicy`, and `fetchClientVersion()`. No `dio` (PRC publisher, see
`general.md` Supply Chain). See spec §4.4.
