name: penguin_core
description: Cross-cutting config, Result/Failure, logging, and provider interfaces shared by every penguinm package.
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.2 <4.0.0'
  flutter: '>=3.44.8'

dependencies:
  flutter_riverpod: 3.4.3
  shared_preferences: 2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: 1.0.5
  penguin_lints:
    path: ../penguin_lints
