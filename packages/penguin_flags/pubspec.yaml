name: penguin_flags
description: FeatureFlags (PostHog + license tier) with offline cache and the FeatureGate widget.
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.12.2 <4.0.0'
  flutter: '>=3.44.8'

dependencies:
  flutter:
    sdk: flutter
  penguin_core:
    path: ../penguin_core
  http: 1.6.0
  shared_preferences: 2.5.5
  flutter_riverpod: 3.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: 1.0.5
  penguin_lints:
    path: ../penguin_lints
  penguin_testing:
    path: ../penguin_testing
