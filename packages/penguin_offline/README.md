# penguin_offline

`ConnectivityMonitor`, `OfflineStore`/`SqliteOfflineStore` (raw parameterised
`package:sqlite3` SQL — no `drift`, no codegen), `SyncQueue` with dead-letter
surfacing, `ConnectivityBanner`, `StaleDataChip`. See spec §4.7.
