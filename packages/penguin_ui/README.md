# penguin_ui

`PenguinTheme`, `FormFactor`, `ResponsiveScaffold`, `AdaptiveLayout`,
`ErrorView`/`LoadingView`/`EmptyView`. Wraps `flutter_libs`' "Elder" widgets
so they share one theme. Golden tests at phone/tablet/expanded sizes. See
spec §4.9.
